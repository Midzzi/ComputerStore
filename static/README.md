Static preview

Open `index.html` in a browser to view a static, offline-friendly preview of the frontend. The `images/` folder should be added next to these files and contain the product images from the original repo's `attached_assets/generated_images/` folder.

If you want, I can copy the images from the previous `attached_assets` into `static/images` — say `yes` and I'll restore them here.